<?php 

    // Operator Arithmetic  +, -, *, /
    // $a = 10;
    // $b = 20;
    // $total = $a + $b;

    // echo $total;

    // Operator Assignment bentuk sederhana aritmatika +=, -=, /=, *=
    // $a = 10;
    // $b = 20;

    // $total = 10;
    // $total = $total + $a;
    // $total += $a;

    // echo $total;

    // Operator Comparison/pembanding >, <, >=, <=
    // $a = 10;
    // $b = 20;

    // $total = $a > $b;
    // echo var_dump($total);

    // Operator Increment & Decrement ++, --
    // $a = 10;

    // $a++;
    // echo $a;

    // Operator String 
    $a = 'Hello';
    $b = ' World';

    // $total = $a . $b;
    $a .= $b;
    echo $a;

?>